# AGENTS.md — `cr-bio/PUBLISHED/biol-1`

Local-only path under `projects/ongoing/Teaching/cr-bio` (root `.gitignore` `projects/*`); never committed.
## Layout
- `module-XX-*/` — published per-module deliverables.
- `modules/` — same modules with per-module `resources/generated/` figures.
- Collections: `course/` (syllabus+schedule), `dashboards/`, `homework/`, `labs/`, `lectures/` (SRT captions), `slides/`, `practice_tests/`, `module_keys/`.
- Aggregates: `ALL_FILES/` (everything in one flat folder), `full_flat/` (flat mirror with prefixed names).

## Invariants
- GENERATED by the publish step from `course_development/` + `output/`. Do not hand-edit.
