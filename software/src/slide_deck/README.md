# slide_deck

Deterministic BIOL-1 slide generation from structured `module.toml` manifests.

The package renders full and notes HTML decks and converts them to PDF with WeasyPrint. It reuses the module-content schema, including generated concept-map, process-model, and retrieval-card SVG assets.
