{
  "batch": "dependency_health",
  "batch_index": 7,
  "assessments": {"dependency_health": 62.0},
  "dimension_notes": {
    "dependency_health": {
      "evidence": [
        "pyproject.toml declares both 'markdown>=3.5.0' and 'markdown2>=2.4.0' as production dependencies, but markdown2 is never imported anywhere in software/src/",
        "pyproject.toml declares 'gtts>=2.5.0' as a production dependency, but the actual TTS implementation in software/src/text_to_speech/utils.py uses macOS 'say' + ffmpeg via subprocess — gTTS is not imported anywhere in software/src/",
        "software/tests/test_dependencies.py line 19 tests 'markdown2' as a required dep, which likely explains why it appears in pyproject.toml despite zero production usage",
        "software/src/text_to_speech/main.py docstrings reference gTTS ('currently not used with gTTS', 'gTTS doesn't support speed/pitch adjustment directly') indicating the original gTTS implementation was replaced by the macOS say approach but the dependency was not removed",
        "pypdf is used only in a single lazy import inside format_conversion/utils.py convert_pdf_to_text — a heavy dependency used for one narrow operation",
        "python-docx is used only inside format_conversion/utils.py (two lazy imports in convert_markdown_to_docx and convert_docx_to_markdown) — another heavy dep with a narrow call site",
        "pydub is used only in speech_to_text/utils.py for WAV conversion before SpeechRecognition — a focused use but still a heavy audio-manipulation library for a single format conversion step",
        "requests is appropriately scoped to canvas_integration only",
        "markdown library (not markdown2) is legitimately used in three modules: lab_manual/utils.py, html_website/utils.py, and markdown_to_pdf/utils.py"
      ],
      "impact_scope": "module",
      "fix_scope": "single_edit",
      "confidence": "high",
      "issues_preventing_higher_score": "Two ghost production dependencies (markdown2, gtts) are declared but never used in production code. Both inflate install footprint and create false signals about what the system requires. The gtts ghost is particularly confusing because docstrings still reference gTTS as if it were the active implementation."
    }
  },
  "dimension_judgment": {
    "dependency_health": {
      "strengths": [
        "All actively used libraries (markdown, weasyprint, requests, pydub, speechrecognition, python-docx, pypdf) have a clear, identifiable use site in production source",
        "No circular import cycles are present in the import graph — the layered architecture keeps dependencies flowing in one direction",
        "requests is tightly scoped to the canvas_integration subsystem with no leakage to other modules",
        "No version conflicts or multiple libraries serving the same HTTP purpose — a single requests library for all HTTP use"
      ],
      "issue_character": "The primary issues are ghost dependencies: production deps that were declared (gtts, markdown2) but whose corresponding import sites were removed when the implementation changed, leaving the pyproject.toml stale and misleading.",
      "score_rationale": "The codebase has two clear ghost dependencies in production requirements — gtts was replaced by macOS say/ffmpeg but the package was never removed, and markdown2 is declared alongside the actively used markdown library with zero production usage. These are direct dependency_health defects: unnecessary entries in dependencies[] inflate install cost and mislead readers about what the system depends on. The remaining deps are appropriately scoped with no overlap or version conflicts, which prevents a lower score. The score of 62 reflects a mostly clean picture marred by two easily-fixed but substantive declaration errors."
    }
  },
  "issues": [
    {
      "dimension": "dependency_health",
      "identifier": "ghost_dep_gtts",
      "summary": "gtts declared as production dependency but never imported in production source",
      "related_files": [
        "software/pyproject.toml",
        "software/src/text_to_speech/utils.py",
        "software/src/text_to_speech/main.py"
      ],
      "evidence": [
        "pyproject.toml line 10: 'gtts>=2.5.0' is in [project.dependencies]",
        "software/src/text_to_speech/utils.py implements text_to_speech_audio() using subprocess.run(['say', ...]) and subprocess.run(['ffmpeg', ...]) — no gtts import exists anywhere in software/src/",
        "software/src/text_to_speech/main.py docstrings still say 'currently not used with gTTS' and 'gTTS doesn't support speed/pitch adjustment directly', confirming gtts was the former implementation but has been replaced without removing the dependency declaration"
      ],
      "suggestion": "Remove 'gtts>=2.5.0' from [project.dependencies] in pyproject.toml and update the stale docstring references in text_to_speech/main.py to describe the current macOS say + ffmpeg approach",
      "confidence": "high",
      "impact_scope": "module",
      "fix_scope": "single_edit",
      "root_cause_cluster": "stale_dependency_after_implementation_swap"
    },
    {
      "dimension": "dependency_health",
      "identifier": "ghost_dep_markdown2",
      "summary": "markdown2 declared as production dependency alongside markdown but is never imported in production code",
      "related_files": [
        "software/pyproject.toml",
        "software/tests/test_dependencies.py"
      ],
      "evidence": [
        "pyproject.toml line 16: 'markdown2>=2.4.0' is in [project.dependencies]",
        "Grep for 'import markdown2' or 'from markdown2' across software/src/ returns zero matches",
        "software/tests/test_dependencies.py line 19 tests 'markdown2' as a required dep — this test appears to be the only reason the entry persists, but the test itself is validating a non-requirement",
        "software/src/lab_manual/utils.py, software/src/html_website/utils.py, and software/src/markdown_to_pdf/utils.py all 'import markdown' (the other library) with no markdown2 usage anywhere"
      ],
      "suggestion": "Remove 'markdown2>=2.4.0' from [project.dependencies] in pyproject.toml and remove or correct the corresponding check in test_dependencies.py",
      "confidence": "high",
      "impact_scope": "module",
      "fix_scope": "single_edit",
      "root_cause_cluster": "stale_dependency_after_implementation_swap"
    }
  ],
  "retrospective": {
    "root_causes": [
      "Implementation was migrated (gTTS -> macOS say/ffmpeg; markdown2 -> markdown) without updating pyproject.toml, leaving ghost entries in production dependencies"
    ],
    "likely_symptoms": [
      "test_dependencies.py validates markdown2 as a required dependency — this test is a symptom of the ghost dep rather than a quality gate"
    ],
    "possible_false_positives": []
  }
}
